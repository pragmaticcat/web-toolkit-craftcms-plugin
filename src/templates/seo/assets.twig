{% extends 'pragmatic-web-toolkit/seo/_layout' %}
{% import '_includes/forms' as forms %}

{% set title = 'Assets'|t('pragmatic-web-toolkit') %}
{% set selectedTab = 'assets' %}

{% set crumbs = [
  { label: 'SEO', url: url('pragmatic-toolkit/seo') },
  { label: 'Assets'|t('pragmatic-web-toolkit'), url: url('pragmatic-toolkit/seo/assets', { site: selectedSite.handle }) },
] %}

{% block content %}
  {% css %}
    .img-preview-wrap { position: relative; display: inline-block; }
    .img-preview-popover {
      display: none;
      position: absolute;
      left: 100%;
      top: 50%;
      transform: translateY(-50%);
      margin-left: 10px;
      background: #fff;
      border: 1px solid #d1d5db;
      border-radius: 8px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.12);
      padding: 6px;
      z-index: 100;
      pointer-events: none;
      white-space: nowrap;
    }
    .img-preview-popover img {
      display: block;
      max-width: 200px;
      max-height: 200px;
      width: auto;
      height: auto;
      border-radius: 4px;
    }
    .img-preview-wrap:hover .img-preview-popover { display: block; }
    .pwt-row-actions {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }
    .pwt-ai-controls {
      display: flex;
      flex-direction: column;
      gap: 10px;
      min-width: 280px;
    }
  {% endcss %}

  {% if not canManageAssets %}
    {% include 'pragmatic-web-toolkit/_partials/tier-lock' with {
      tier: 'pro',
      heading: 'Requires Pro version',
      message: 'SEO asset metadata management is available in Pro.'
    } %}
  {% endif %}

  <form id="assets-filter-form" method="get" action="{{ url('pragmatic-toolkit/seo/assets') }}" accept-charset="UTF-8" style="margin-bottom: 16px;">
    <input type="hidden" name="site" value="{{ selectedSite.handle }}">
    <input type="hidden" name="page" value="1">
    <div class="flex" style="gap: 12px; align-items: flex-end;">
      {{ forms.selectField({
        id: 'used-filter',
        name: 'used',
        label: 'Usage filter'|t('pragmatic-web-toolkit'),
        value: usedFilter ?? 'used',
        options: [
          { label: 'View all'|t('pragmatic-web-toolkit'), value: 'all' },
          { label: 'Only used'|t('pragmatic-web-toolkit'), value: 'used' },
          { label: 'Only unused'|t('pragmatic-web-toolkit'), value: 'unused' },
        ],
      }) }}
      <div>
        <label for="perPage">{{ 'Per page'|t('pragmatic-web-toolkit') }}</label>
        <div class="select">
          <select id="perPage" name="perPage">
            {% for option in [50, 100, 250] %}
              <option value="{{ option }}" {{ option == perPage ? 'selected' : '' }}>{{ option }}</option>
            {% endfor %}
          </select>
        </div>
      </div>
    </div>
  </form>

  <p class="light">
    {% if gemFeatureEnabled %}
      {{ 'Generate with AI opens a prompt you can paste into a chat that uses your Gemini Gem.'|t('pragmatic-web-toolkit') }}
    {% else %}
      {{ 'Generate with AI opens a single self-contained prompt you can paste directly into Gemini.'|t('pragmatic-web-toolkit') }}
    {% endif %}
  </p>

  {% if rows is empty %}
    <p>{{ 'No assets to show with the current filter.'|t('pragmatic-web-toolkit') }}</p>
  {% else %}
    <fieldset {{ not canManageAssets ? 'disabled' : '' }}>
      <table class="data fullwidth">
        <thead>
          <tr>
            <th>{{ 'Used'|t('pragmatic-web-toolkit') }}</th>
            <th>{{ 'Assets'|t('pragmatic-web-toolkit') }}</th>
            <th>{{ 'AI indications'|t('pragmatic-web-toolkit') }}</th>
            <th>{{ 'Generate with AI'|t('pragmatic-web-toolkit') }}</th>
            <th>{{ 'Title'|t('pragmatic-web-toolkit') }}</th>
            <th>{{ 'Alt'|t('pragmatic-web-toolkit') }}</th>
            <th>{{ 'Save'|t('pragmatic-web-toolkit') }}</th>
          </tr>
        </thead>
        <tbody>
          {% for row in rows %}
            {% set asset = row.asset %}
            {% set rowFormId = 'save-asset-' ~ asset.id %}
            <tr>
              <td>
                {% if row.isUsed %}
                  <span class="status enabled" title="{{ 'Used'|t('pragmatic-web-toolkit') }}"></span>
                {% else %}
                  <span class="status" title="{{ 'Unused'|t('pragmatic-web-toolkit') }}"></span>
                {% endif %}
              </td>
              <td>
                <span class="img-preview-wrap">
                  <a href="{{ asset.cpEditUrl }}" class="js-open-asset" data-asset-id="{{ asset.id }}" data-site-id="{{ asset.siteId }}">{{ asset.filename }}</a>
                  {% if asset.url %}
                    <span class="img-preview-popover">
                      <img src="{{ asset.url }}" alt="{{ asset.title }}" loading="lazy">
                    </span>
                  {% endif %}
                </span><br>
                <code>ID {{ asset.id }}</code>
              </td>
              <td>
                <textarea
                  class="text fullwidth"
                  form="{{ rowFormId }}"
                  name="assets[{{ asset.id }}][fields][__ai_instructions__]"
                  rows="4"
                  placeholder="{{ 'Optional stored instructions for this asset AI prompt.'|t('pragmatic-web-toolkit') }}"
                >{{ row.fieldValues.__ai_instructions__ ?? '' }}</textarea>
              </td>
              <td>
                    <button
                      type="button"
                      class="btn js-generate-asset-row"
                      data-asset-id="{{ asset.id }}"
                      data-site-id="{{ asset.siteId }}"
                      data-row-form="{{ rowFormId }}"
                      title="{{ 'Generate with AI'|t('pragmatic-web-toolkit') }}"
                      aria-label="{{ 'Generate with AI'|t('pragmatic-web-toolkit') }}"
                    >
                      <span aria-hidden="true">AI</span>
                    </button>
                  </td>
              <td>
                <textarea
                  class="text fullwidth"
                  form="{{ rowFormId }}"
                  name="assets[{{ asset.id }}][title]"
                  rows="3"
                >{{ asset.title }}</textarea>
              </td>
              <td>
                <textarea
                  class="text fullwidth"
                  form="{{ rowFormId }}"
                  name="assets[{{ asset.id }}][fields][__native_alt__]"
                  rows="3"
                >{{ row.fieldValues.__native_alt__ ?? '' }}</textarea>
              </td>
              <td>
                <div class="pwt-row-actions">
                  <button type="submit" class="btn submit" form="{{ rowFormId }}" name="saveRowId" value="{{ asset.id }}">{{ 'Save row'|t('pragmatic-web-toolkit') }}</button>
                </div>
              </td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    </fieldset>

    {% for row in rows %}
      {% set asset = row.asset %}
      {% set rowFormId = 'save-asset-' ~ asset.id %}
      <form id="{{ rowFormId }}" method="post" accept-charset="UTF-8">
        {{ csrfInput() }}
        {{ actionInput('pragmatic-web-toolkit/seo/save-assets') }}
        <input type="hidden" name="site" value="{{ selectedSiteId ?? selectedSite.id }}">
        {{ redirectInput(url('pragmatic-toolkit/seo/assets', {
          site: selectedSite.handle,
          used: usedFilter ?? 'used',
          perPage: perPage,
          page: page
        })) }}
      </form>
    {% endfor %}

    <div class="flex justify-between align-center" style="margin-top: 16px;">
      <div>{{ total }} {{ 'total'|t('pragmatic-web-toolkit') }}</div>
      <div class="flex" style="gap: 8px; align-items: center;">
        {% set prevPage = page > 1 ? page - 1 : 1 %}
        {% set nextPage = page < totalPages ? page + 1 : totalPages %}
        <a class="btn {{ page == 1 ? 'disabled' : '' }}" href="{{ url('pragmatic-toolkit/seo/assets', { site: selectedSite.handle, used: usedFilter ?? 'used', perPage: perPage, page: prevPage }) }}">{{ 'Prev'|t('pragmatic-web-toolkit') }}</a>
        <div>{{ 'Page'|t('pragmatic-web-toolkit') }} {{ page }} / {{ totalPages }}</div>
        <a class="btn {{ page == totalPages ? 'disabled' : '' }}" href="{{ url('pragmatic-toolkit/seo/assets', { site: selectedSite.handle, used: usedFilter ?? 'used', perPage: perPage, page: nextPage }) }}">{{ 'Next'|t('pragmatic-web-toolkit') }}</a>
      </div>
    </div>
  {% endif %}
{% endblock %}

{% js %}
  function openManualPromptModal(title, promptText) {
    const modalEl = document.createElement('div');
    modalEl.className = 'modal fitted';
    modalEl.innerHTML =
      '<div class="body">' +
        '<div class="content">' +
          '<h2 style="margin-top:0;">' + title + '</h2>' +
          '<p>{% if gemFeatureEnabled %}{{ "Paste this prompt into a chat that uses your Gemini Gem. Ask it to return only the requested JSON."|t("pragmatic-web-toolkit")|e("js") }}{% else %}{{ "Paste this single prompt directly into Gemini. It already includes the strategy instructions. Ask it to return only the requested JSON."|t("pragmatic-web-toolkit")|e("js") }}{% endif %}</p>' +
          '<textarea class="text fullwidth js-manual-prompt-output" rows="18" readonly style="font-family: Menlo, Monaco, Consolas, monospace;">' + Craft.escapeHtml(promptText) + '</textarea>' +
        '</div>' +
      '</div>' +
      '<div class="footer">' +
        '<div class="btngroup">' +
          '<button type="button" class="btn" data-action="copy">{{ "Copy prompt"|t("pragmatic-web-toolkit")|e("js") }}</button>' +
          '<button type="button" class="btn" data-action="close">{{ "Close"|t("pragmatic-web-toolkit")|e("js") }}</button>' +
        '</div>' +
      '</div>';

    const modal = new Garnish.Modal(modalEl, {
      autoShow: true,
      onHide: function() { modal.destroy(); }
    });
    const outputField = modalEl.querySelector('.js-manual-prompt-output');
    const copyBtn = modalEl.querySelector('[data-action="copy"]');
    const closeBtn = modalEl.querySelector('[data-action="close"]');
    if (outputField) {
      outputField.focus();
      outputField.select();
    }

    if (copyBtn) {
      copyBtn.addEventListener('click', async () => {
        try {
          await navigator.clipboard.writeText(promptText);
          Craft.cp.displayNotice('{{ "Prompt copied."|t("pragmatic-web-toolkit")|e("js") }}');
        } catch (error) {
          if (outputField) {
            outputField.focus();
            outputField.select();
          }
          Craft.cp.displayError('{{ "Could not copy automatically. Copy it manually from the textarea."|t("pragmatic-web-toolkit")|e("js") }}');
        }
      });
    }
    if (closeBtn) {
      closeBtn.addEventListener('click', () => modal.hide());
    }
  }

  const assetsFilterForm = document.getElementById('assets-filter-form');
  const usedFilterSelect = assetsFilterForm?.querySelector('#used-filter');
  const perPageSelect = assetsFilterForm?.querySelector('select[name="perPage"]');
  const pageInput = assetsFilterForm?.querySelector('input[name="page"]');
  if (assetsFilterForm && usedFilterSelect) {
    usedFilterSelect.addEventListener('change', () => {
      if (pageInput) pageInput.value = '1';
      assetsFilterForm.submit();
    });
  }
  if (assetsFilterForm && perPageSelect) {
    perPageSelect.addEventListener('change', () => {
      if (pageInput) pageInput.value = '1';
      assetsFilterForm.submit();
    });
  }

  document.querySelectorAll('.js-open-asset').forEach((link) => {
    link.addEventListener('click', (event) => {
      if (typeof Craft === 'undefined' || typeof Craft.createElementEditor !== 'function') {
        return;
      }

      event.preventDefault();
      const assetId = parseInt(link.dataset.assetId || '0', 10);
      const siteId = parseInt(link.dataset.siteId || '0', 10);
      if (!assetId || !siteId) {
        return;
      }

      Craft.createElementEditor('craft\\elements\\Asset', $(link), {
        elementId: assetId,
        siteId: siteId,
      });
    });
  });

  document.querySelectorAll('.js-generate-asset-row').forEach((button) => {
    button.addEventListener('click', async (event) => {
      event.preventDefault();
      if (typeof Craft === 'undefined' || typeof Craft.sendActionRequest !== 'function') {
        return;
      }

      const titleInput = document.querySelector(`textarea[name="assets[${button.dataset.assetId}][title]"]`);
      const altTextarea = document.querySelector(`textarea[name="assets[${button.dataset.assetId}][fields][__native_alt__]"]`);
      const originalLabel = button.textContent;

      button.disabled = true;
      button.classList.add('loading');

      try {
        const response = await Craft.sendActionRequest('POST', 'pragmatic-web-toolkit/seo/generate-asset-metadata', {
          data: {
            assetId: button.dataset.assetId,
            siteId: button.dataset.siteId,
          },
        });

        if (!response.data || !response.data.success) {
          throw new Error(response.data && response.data.error ? response.data.error : 'Could not generate asset metadata.');
        }

        if (response.data.mode === 'manual' && response.data.manualPrompt) {
          openManualPromptModal('{{ "Asset AI Prompt"|t("pragmatic-web-toolkit")|e("js") }}', response.data.manualPrompt);
          return;
        }

      } catch (error) {
        const message = error && error.message ? error.message : `{{ 'Could not generate asset metadata.'|t('pragmatic-web-toolkit')|e('js') }}`;
        Craft.cp.displayError(message);
      } finally {
        button.disabled = false;
        button.classList.remove('loading');
        button.textContent = originalLabel;
      }
    });
  });
{% endjs %}
