{% extends 'pragmatic-web-toolkit/sync/_layout' %}
{% import '_includes/forms' as forms %}

{% set title = 'Options'|t('pragmatic-web-toolkit') %}
{% set selectedTab = 'options' %}

{% block content %}
  {% css %}
    .sync-options-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 18px;
      margin-top: 18px;
    }
    .sync-options-card {
      border: 1px solid #e5e7eb;
      border-radius: 10px;
      padding: 18px;
      background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
    }
    .sync-options-card h2 {
      margin: 0 0 8px;
    }
    @media (max-width: 900px) {
      .sync-options-grid {
        grid-template-columns: 1fr;
      }
    }
  {% endcss %}

  <form method="post" accept-charset="UTF-8">
    {{ csrfInput() }}
    {{ actionInput('pragmatic-web-toolkit/sync/save-options') }}
    {{ redirectInput('pragmatic-toolkit/sync/options') }}

    <div class="sync-options-grid">
      <section class="sync-options-card">
        <h2>{{ 'Support boundary'|t('pragmatic-web-toolkit') }}</h2>
        <p class="light">{{ 'Sync is a manual transfer workflow. Use it only between matching project deployments.'|t('pragmatic-web-toolkit') }}</p>
        <ul>
          <li>{{ 'Manual package transfer only. No direct environment-to-environment sync in this version.'|t('pragmatic-web-toolkit') }}</li>
          <li>{{ 'Only local filesystem asset volumes are supported.'|t('pragmatic-web-toolkit') }}</li>
          <li>{{ 'Database import fully replaces the target database using a pure-PHP MySQL/MariaDB table dump.'|t('pragmatic-web-toolkit') }}</li>
          <li>{{ 'Asset import merges imported files into local volume roots and overwrites same-path files.'|t('pragmatic-web-toolkit') }}</li>
          <li>{{ 'Views, triggers, routines, and events are detected and warned about, but not transferred.'|t('pragmatic-web-toolkit') }}</li>
          <li>{{ 'Target-only asset files remain on disk and may become orphaned after DB restore.'|t('pragmatic-web-toolkit') }}</li>
        </ul>
      </section>

      <section class="sync-options-card">
        <h2>{{ 'Retention'|t('pragmatic-web-toolkit') }}</h2>
        <p class="light">{{ 'Control how long staged uploads, export artifacts, and transfer log rows are kept.'|t('pragmatic-web-toolkit') }}</p>

        {{ forms.textField({
          label: 'Staged upload retention (hours)'|t('pragmatic-web-toolkit'),
          name: 'settings[stagedUploadRetentionHours]',
          value: settings.stagedUploadRetentionHours,
          type: 'number',
          min: 1,
          max: 72,
        }) }}

        {{ forms.textField({
          label: 'History retention (days)'|t('pragmatic-web-toolkit'),
          name: 'settings[historyRetentionDays]',
          value: settings.historyRetentionDays,
          type: 'number',
          min: 1,
          max: 365,
        }) }}

        {{ forms.textField({
          label: 'Export artifact retention (hours)'|t('pragmatic-web-toolkit'),
          name: 'settings[exportArtifactRetentionHours]',
          value: settings.exportArtifactRetentionHours,
          type: 'number',
          min: 1,
          max: 168,
        }) }}
      </section>

      <section class="sync-options-card">
        <h2>{{ 'Database dump tuning'|t('pragmatic-web-toolkit') }}</h2>
        <p class="light">{{ 'Tune the pure-PHP MySQL/MariaDB dump format for large environments.'|t('pragmatic-web-toolkit') }}</p>

        {{ forms.textField({
          label: 'Insert batch row count'|t('pragmatic-web-toolkit'),
          name: 'settings[insertBatchRowCount]',
          value: settings.insertBatchRowCount,
          type: 'number',
          min: 1,
          max: 5000,
        }) }}

        {{ forms.textField({
          label: 'Select chunk size'|t('pragmatic-web-toolkit'),
          name: 'settings[selectChunkSize]',
          value: settings.selectChunkSize,
          type: 'number',
          min: 1,
          max: 10000,
        }) }}
      </section>
    </div>

    <div class="buttons" style="margin-top: 18px;">
      <button class="btn submit" type="submit">{{ 'Save'|t('pragmatic-web-toolkit') }}</button>
    </div>
  </form>
{% endblock %}
